<fieldset>
  <fieldset>
    <label style="height:20px;">Alternatively</label>
  </fieldset>

  <div class="line"></div>

  <div class="upload">
    <label for="<%= id %>"><%= uploadmessage %> (20MB limit) </label>
    <br/>
    <input id="<%= id %>" name="<%= name %>" type="file" accept="text/*, application/pdf, .xls, .xlsx"/>
    <%= render "errors", {errors: @errors, key: name} %>
  </div>
</fieldset>
