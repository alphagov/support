<div class="alert alert-info alert-block">
  <p>
    Changes and new features need to be assessed against the GOV.UK roadmap.
    Give as much detail as you can about the user need and evidence for your
    request.
  </p>
</div>

<div class="form-group">
  <span class="form-label">
    <%= f.label :title, "Title of request" %>
  </span>
  <span class="form-wrapper">
    <%= f.text_field :title, placeholder: "New need request", class: "input-md-6 form-control" %>
  </span>
</div>

<fieldset>
  <legend>
    <span>Details of the new feature/need</span>
  </legend>

  <div class="form-group">
    <span class="form-label">
      <%= f.label :user_need do %>
        What is the product request or technical advice you need?<abbr title="required">*</abbr>
      <% end %>
    </span>
    <span class="form-wrapper">
      <%= f.text_area :user_need, required: true, aria: { required: true }, class: "input-md-6 form-control", rows: 6, cols: 50 %>
    </span>
  </div>

  <div class="form-group">
    <span class="form-label">
      <%= f.label :feature_evidence, "If this is a request for a new feature, what evidence do you have to support the request?" %>
    </span>
    <span class="form-wrapper">
      <%= f.text_area :feature_evidence, class: "input-md-6 form-control", rows: 6, cols: 50 %>
    </span>
  </div>
</fieldset>

<%= render partial: "support/attachment_instructions" %>

<%= render partial: "support/collaborators", locals: { f: f } %>
