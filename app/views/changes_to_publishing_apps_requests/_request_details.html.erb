<%= f.input :title, label: "Title of request", input_html: { class: "input-md-6", placeholder: "New need request" } %>

<%= f.inputs name: "Details of the new feature/need" do %>

  <%= f.input :user_need, as: :text, required: true, label: "What is the product request or technical advice you need?", input_html: { :"aria-required" => true, :class => "input-md-6", :rows => 6, :cols => 50 } %>

  <%= f.input :feature_evidence, as: :text, label: "If this is a request for a new feature, what evidence do you have to support the request?", input_html: { :class => "input-md-6", :rows => 6, :cols => 50 } %>

<% end %>

<%= render partial: "support/attachment_instructions" %>

<%= render partial: "support/collaborators", locals: { f: f } %>
